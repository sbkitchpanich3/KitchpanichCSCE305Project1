<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sbkitchpanich.kitchpanichcsce305project1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".PageOne">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PageTwo"
            android:label="pagetwo"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageThree"
            android:label="pagethree"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageFour"
            android:label="pagefour"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageFive"
            android:label="pagefive"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageSix"
            android:label="pagesix"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageSeven"
            android:label="pageseven"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageEight"
            android:label="pageeight"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageNine"
            android:label="pagenine"
            android:theme="@style/AppTheme" />

        <activity android:name=".PageTen"
            android:label="pageten"
            android:theme="@style/AppTheme" />

    </application>

</manifest>